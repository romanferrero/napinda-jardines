/**
 * Galería de proyectos (fallback).
 *
 * Este array se usa solo como respaldo si Firestore está vacío o no
 * responde. Los proyectos reales se administran desde el panel de admin
 * y se guardan en Firebase. Dejalo vacío para mostrar únicamente lo que
 * venga de Firebase.
 */

export const gallery = []
